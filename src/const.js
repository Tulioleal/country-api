const DATE_NAGGER_ROUTE = "https://date.nager.at/api/v3"
const COUNTRIES_NOW = "https://countriesnow.space/api/v0.1"
const PORT = process.env.PORT || 3000;

module.exports = {
  DATE_NAGGER_ROUTE,
  COUNTRIES_NOW,
  PORT,
}